import React from "react";
import classes from "./Bind.module.css";

const Bind = (props) => {
  return <div className={classes.Bind}>
    <label>A</label>
    <input></input>
  </div>;
};

export default Bind;
